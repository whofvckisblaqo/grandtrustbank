// Grand Trust Bank's institution-wide ABA routing number.
// Real banks use ONE routing number for the whole institution — it identifies
// the bank itself, while the account number identifies the individual account.
// Change this value if you need a different number; it's used everywhere the
// routing number is displayed.
export const GTB_ROUTING_NUMBER = '071925678';